I'm rethinking how this harness works. Instead of it providing it's own implementation flow I think it would be better if this was more of a harness that provides tools to whatever system you use for implementation for example i tend to use speckit, i want this to to provide a bunch of useful features that speckit can take advantage of to increase the likelyhood that it generates a throrough well implemented feature on the first try with minimum reuse. essentially this tool set should provide skills/hooks/etc that enhance whatever other tools i decide to use day to day rather than trying to be it's own tool. essentially turning the codebase into a knowledge base. I still think we want to keep some of the skills /forge.research /forge.ask /forge.detect-stack /forge.docs-sync /forge.find-reuse /forge.review /forge.stats /forge.survey /forge.tdd are all useful but i don't think we need the implementatation stuff i feel like we're just recreating speckit, so why not just use speckit.
